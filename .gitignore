# Editores / IDEs
.vscode/
.idea/
*.swp

# Sistema
.DS_Store
Thumbs.db

# Dependências
node_modules/

# Variáveis de ambiente — NUNCA commitar, contém o token do proxy
.env
.env.local
.env.*.local

# Pasta gerada pelo Vite no build — não precisa versionar
dist/

# Roda no script.google.com — mantido localmente só como referência.
proxy/
